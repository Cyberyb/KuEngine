# 工作日志 - 2026-04-14

## 1. 今日完成事项

### 1.1 文档对齐与产品化规划优化
- 对齐了核心文档与当前代码实现，修复了历史漂移（例如接口签名与模块职责描述不一致）。
- 更新了项目状态描述：v0.1.x 基线可运行，进入 v0.2 规划阶段。
- 以“基于 Vulkan 的快速算法验证框架”为目标，重构版本路线图，补充了可执行里程碑与验收标准。

涉及文档：
- `README.md`
- `CHANGELOG.md`
- `docs/design/00-overview.md`
- `docs/design/01-rhi-layer.md`
- `docs/design/02-render-pass.md`
- `docs/design/04-triangle-example-tech.md`

### 1.2 新增 Cube 示例（examples/cube）
- 新增 `CubeApp` 示例与独立 CMake 配置、运行脚本、shader 编译脚本。
- 初版完成：鼠标左键拖拽旋转立方体、ImGui 实时调色。
- 根据验证反馈迭代为双模式：
  - 默认渲染立方体六个面（实心模式）。
  - 线框模式作为 ImGui 开关选项。
- 实现方式：同一 Pass 内维护实心/线框两套管线，按 UI 状态切换 draw path。

新增目录：
- `examples/cube/`

新增文件：
- `examples/cube/CMakeLists.txt`
- `examples/cube/main.cpp`
- `examples/cube/CubePass.h`
- `examples/cube/CubePass.cpp`
- `examples/cube/run_cube.bat`
- `examples/cube/shaders/cube.vert`
- `examples/cube/shaders/cube.frag`
- `examples/cube/shaders/compile_shaders.bat`

同时更新：
- `examples/CMakeLists.txt`（接入 `add_subdirectory(cube)`）

### 1.3 构建与运行验证
- 已完成 `CubeApp` Debug 构建验证。
- 已通过 `run_cube.bat` 完成配置、构建、shader 编译与示例启动。
- 结果：示例可运行，支持拖拽旋转与颜色调节，线框开关生效。

## 2. 当前阶段评估（面向 v0.2 前）

### 2.1 结论
- 当前框架已具备继续推进 3D 算法验证示例的基础能力。
- 对于“快速验证”目标，现阶段路线正确：先确保示例扩展效率与可观测性，再推进更复杂渲染能力。

### 2.2 仍需补强
- 深度附件与深度测试的标准化支持（为更复杂 3D 场景做准备）。
- 更系统的性能观测能力（GPU 时间、帧时间曲线、可复现实验配置）。
- 自动化测试覆盖扩展（当前示例验证以构建和手工运行为主）。

## 3. 下一步建议
- 为 Cube 示例补充使用文档（含交互说明、线框模式说明、常见问题）。
- 规划 v0.2 RenderGraph Alpha 的最小落地路径（先 2-3 Pass，后资源依赖自动化）。
- 在 RHI 层评估深度资源封装方案，作为更多 3D 示例的公共基础。

## 4. v0.2 阶段0开发（范围冻结）

### 4.1 完成内容
- 完成 v0.2 范围冻结（In Scope / Out of Scope）。
- 完成 v0.2 验收门禁定义（功能、工程、可观测性）。
- 完成阶段1-6执行拆分、周计划建议与风险应对。
- 新增执行计划文档：`docs/design/06-v0.2-execution-plan.md`。
- 总览文档同步阶段0冻结结论：`docs/design/00-overview.md`。

### 4.2 产出价值
- 进入阶段1前已具备统一目标、统一验收标准、统一优先级。
- 降低范围漂移与返工风险，确保 v0.2 以可交付 Alpha 为第一目标。

### 4.3 下一步执行点
- 按执行计划进入阶段1：落地 RenderGraph 最小数据模型与兼容桥接层。

## 5. v0.2 阶段1开发（最小数据模型）

### 5.1 完成内容
- 新增 RenderGraph 基础结构：Pass 节点、资源句柄、读写声明、执行顺序缓存。
- 新增 `RenderGraphBuilder` 声明接口：`createResource/importExternal/read/write`。
- `RenderPass` 增加 `setup(RenderGraphBuilder&)`，并保持对旧 `setup()` 的兼容桥接。
- `RenderPipeline::compile()` 已切换到图声明路径，`execute()` 优先按编译序执行。
- TrianglePass 与 CubePass 已接入最小声明：写入 `SwapChainColor` 外部资源。

### 5.2 验证结果
- CMake Tools 在当前会话无法完成配置（工具侧返回“无法配置项目”）。
- 通过问题面板对本次改动文件进行语义检查，未发现编译错误。

### 5.3 下一步执行点
- 进入阶段2：实现依赖图构建、拓扑排序、循环依赖检测。

## 6. v0.2 阶段2开发（依赖编译器）

### 6.1 完成内容
- RenderGraph 编译阶段已支持资源冲突依赖边构建。
- 新增显式依赖声明：`RenderGraphBuilder::dependsOn(passName)`。
- `compile()` 已切换为拓扑排序执行序生成。
- 新增循环依赖检测与错误提示。
- 新增基础 barrier 计划结构（RAW/WAR/WAW）。
- RenderPipeline 编译日志补充：执行序、依赖边、barrier 计划摘要。

### 6.2 测试覆盖
- 新增 `tests/core/test_render_graph.cpp`，覆盖：
  - 显式依赖排序
  - 资源依赖和 barrier 计划生成
  - 缺失依赖报错
  - 循环依赖检测

### 6.3 验证结果
- 问题面板静态检查：本次改动文件无错误。
- CMake Tools 在当前会话仍无法配置项目，`RunCtest_CMakeTools` 无法执行测试。

### 6.4 下一步执行点
- 进入阶段3：将 barrier 计划接入 `CommandList::imageBarrier` 与执行器路径。

## 7. v0.2 阶段3开发（执行器对接 RHI）

### 7.1 完成内容
- RenderPipeline 执行阶段接入 barrier 计划消费逻辑。
- 新增外部资源绑定接口：按 RenderGraph 资源名绑定图像与当前布局。
- barrier 发射接入 `CommandList::imageBarrier`。
- `CommandList::imageBarrier` 补充常见布局的访问掩码填充。
- Triangle/Cube 示例移除重复的手写屏障函数，统一走 `CommandList::imageBarrier`。

### 7.2 构建与测试
- 通过 `cmake --preset vs2022-vcpkg` 配置。
- 通过 `cmake --build --preset debug` 全量构建（KuEngine + CubeApp + TriangleApp + test_core）。
- 通过 `ctest --preset debug`。

### 7.3 下一步执行点
- 进入阶段4：构建至少 3 Pass 的 Alpha 场景，并验证多 Pass 下的调度与 barrier 行为。

## 8. v0.2 阶段4开发（三 Pass Alpha 场景）

### 8.1 完成内容
- 新增 `examples/alpha3pass/` 示例与构建接入。
- 新增 `Alpha3PassApp` 三个 Pass：`Background`、`MainTriangle`、`Accent`。
- 三个 Pass 全部接入 RenderGraph 资源声明与显式依赖链（Background -> MainTriangle -> Accent）。
- 每个 Pass 暴露 UI 参数（颜色、偏移、缩放），用于快速算法参数验证。
- 新增独立 shader 与一键脚本：`run_alpha3pass.bat`。
- 新增使用文档：`docs/usage/alpha3pass-example.md`。

### 8.2 构建与测试
- 通过 `cmake --build --preset debug --target Alpha3PassApp`。
- 通过 `ctest --preset debug`。

### 8.3 下一步执行点
- 进入阶段5：补强可观测性面板与测试覆盖，尤其是 barrier 执行摘要可视化。

## 9. v0.2 阶段5开发（可观测与测试补强）

### 9.1 完成内容
- RenderPipeline 新增 `RenderGraph Debug` 调试面板。
- 调试面板可展示 compile 摘要、依赖边、barrier 计划与最近一帧执行摘要。
- RenderPipeline 执行期新增 barrier 事件记录（applied/skipped + 原因）。
- RenderGraph 单测补强：新增 hazard 类型覆盖与 external 资源标记验证。
- 新增回归说明文档：`docs/usage/v0.2-regression-checks.md`。

### 9.2 构建与测试
- 通过 Debug 构建。
- 通过 `ctest --preset debug`。

### 9.3 下一步执行点
- 进入阶段6：发布收尾（文档一致性复核、门禁勾选、发布说明）。

---
记录人：GitHub Copilot（GPT-5.3-Codex）
