# 工作日志 - 2026-04-12

## 1. 今日完成事项

### 1.1 渲染与稳定性修复
- 修复了交换链图像首帧布局问题：引入每张图像布局跟踪，确保从 `UNDEFINED -> COLOR_ATTACHMENT_OPTIMAL -> PRESENT_SRC_KHR` 的转换正确。
- 修复了资源销毁顺序问题：确保 `SwapChain` 相关对象销毁后再销毁 `VkSurfaceKHR`，消除 Vulkan Validation 关键报错。
- 将示例三角形从“全屏覆盖三角形”调整为“可见几何三角形”，便于验证渲染结果。

### 1.2 UI 能力建设
- 完成 Dear ImGui 在 Vulkan + GLFW 路径下的接入。
- 新增左上角运行信息面板，支持实时显示 FPS / 帧时。
- 新增三角形颜色实时调节功能：通过 UI 调整参数，并在渲染阶段通过 push constants 生效。

### 1.3 架构收敛（核心）
- 将通用 UI 能力收敛到 `UIOverlay`：统一管理 ImGui 生命周期、每帧开始与渲染提交、swapchain 重建同步。
- 将业务 UI 收敛到 `RenderPass::drawUI()`：示例中由 `TrianglePass::drawUI()` 承担颜色面板。
- 主循环改为只使用框架接口（`UIOverlay + RenderPipeline::drawUI()`），减少示例层耦合。

### 1.4 文档与工程
- 新增 UI 教学文档，说明 UI 层架构和自定义 UI 编写流程。
- 更新依赖与构建配置：启用 `imgui` 的 `glfw-binding` 和 `vulkan-binding` 特性。
- 保持示例可编译可运行，完成构建与运行冒烟验证。

## 2. 当前版本功能清单（v0.1.0）

### 2.1 基础框架能力
- Vulkan 1.3 初始化、物理设备选择、逻辑设备与队列创建。
- GLFW 窗口系统与事件处理。
- 交换链创建与重建流程。
- 命令缓冲录制、同步对象管理（Fence/Semaphore）。

### 2.2 渲染能力
- Dynamic Rendering 渲染路径（`vkCmdBeginRendering` / `vkCmdEndRendering`）。
- 最小图形管线封装（shader/pipeline/layout）。
- Triangle 示例完整帧流程（acquire -> record -> submit -> present）。

### 2.3 UI 能力
- UIOverlay 框架层封装（ImGui GLFW+Vulkan 后端）。
- 通用统计面板（FPS、帧时）。
- RenderPass 级参数面板（TrianglePass 颜色实时调节）。

### 2.4 工程能力
- CMake + vcpkg 构建链路稳定。
- `run_triangle.bat` 一键配置/编译/运行路径可用。
- 设计文档持续更新，支持后续迭代追踪。

## 3. 明日建议事项
- 将 `RenderPipeline::drawUI()` 增加分组与折叠头，提升多 Pass 可维护性。
- 增加通用调试面板（GPU 时间统计、帧时间曲线）。
- 规划 v0.2 的 RenderGraph 接入文档与里程碑拆分。

---
记录人：GitHub Copilot（GPT-5.3-Codex）
